{
  "name": "backend",
  "scripts": {
    "build": "tsc --outDir dist/"
  },
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.292.0",
    "@types/aws-lambda": "^8.10.113",
    "cross-env": "^7.0.3",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  }
}
