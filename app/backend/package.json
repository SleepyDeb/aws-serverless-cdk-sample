{
  "name": "backend",
  "scripts": {
    "build": "tsc",    
    "test": "cross-env AWS_PROFILE=personal AWS_REGION=eu-central-1 ORDERS_TABLE_NAME=sample-orders-table jest"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.113",    
    "@types/jest": "^29.4.4",
    "cross-env": "^7.0.3",
    "jest": "^29.5.0",
    "ts-jest": "^29.0.5",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.292.0",
    "@aws-sdk/lib-dynamodb": "^3.292.0"    
  }
}
